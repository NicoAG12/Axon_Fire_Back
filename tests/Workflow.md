npx prisma generate        # regenera Prisma client
npx prisma migrate deploy  # aplica migraciones pendientes
npx ts-node src/index.ts & # levanta servidor en background
npx jest --verbose         # corre todos los tests
